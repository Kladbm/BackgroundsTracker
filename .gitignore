# Node
node_modules/

# Claude Code local config / skills
.claude/

# Downloaded images — fully regenerable by the scraper
public/images/

# Scraped data — regenerable, rewritten by the daily cron re-scrape (tracking
# it in git would create noisy near-empty diffs)
public/data/
